if (CONFIG_ENABLE_DASHBOARD STREQUAL "y")

set(APP_MODULE_PATH ${CMAKE_CURRENT_LIST_DIR})

file(GLOB_RECURSE APP_MODULE_SRCS "${APP_MODULE_PATH}/*.c")

target_sources(${EXAMPLE_LIB} PRIVATE ${APP_MODULE_SRCS})
target_include_directories(${EXAMPLE_LIB} PRIVATE ${APP_MODULE_PATH})

endif()
